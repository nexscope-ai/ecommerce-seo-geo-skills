{
  "name": "schema-markup-generator",
  "version": "1.1.0",
  "description": "Generate strict, evidence-safe ecommerce JSON-LD from verified visible facts.",
  "triggers": ["schema markup", "JSON-LD", "product schema", "generate schema"],
  "files": ["SKILL.md"],
  "metadata_note": "Never invent ratings, reviews, prices, stock, or policies."
}
